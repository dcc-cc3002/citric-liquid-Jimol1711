package cl.uchile.dcc.citric
package model.units.wildunits

/** A class representing a Chicken.
 *
 * The values for each WildUnit are set on it's particular class since they differ from one another.
 *
 */
class Chicken extends AbstractWildUnit(maxHp = 3, offense = -1, defense = -1, evasion = +1, bonusStars = 3) {

  /* The Chicken class has no implementation since it's just an instance of a Wild Unit with default values */

}
