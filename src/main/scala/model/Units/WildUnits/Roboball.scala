package cl.uchile.dcc.citric
package model.Units.WildUnits

/** A class representing a Roboball.
 *
 * The values for each WildUnit are set on it's particular class since they differ from one another.
 *
 */
class Roboball extends AbstractWildUnit(maxHp = 3, offense = -1, defense = +1, evasion = -1, bonusStars = 2) {

  /* The Roboball class has no implementation since it's just an instance of a Wild Unit with default values */

}
